import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  typography: {
    fontFamily: '"ALS Hauss Black", sans-serif',
  },
  palette: {
    primary: {
      main: "#2F80ED",
    },
  },
})

export default theme
